# yt-pipeline

node_modules/
dist/
.env
.env.*
!.env.example

# Project content (channel-specific, NOT part of the pipeline framework)
# The pipeline (agents, commands, scripts, templates) is git-tracked.
# But all project content (research, scripts, storyboards, videos) is local only.
# Each channel instance manages its own content independently.
projects/

# Channel-specific config (template is in templates/channel-config.json)
channel-config.json

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Figma plugin build output
figma-plugin/code.js

# Test/temp files
test_voice.mp3
nul
